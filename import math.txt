import math
def modelo(y,t):
    dydt = a*y*(1+2*math.sin(w*t))
    return dydt
import numpy as np
y_0 = 5
w = 0.007
a = 0.05
t_lista = np.arrange(0, 400, 0.001)
from scipy.integrate import odeint
y_lista = odeint(modelo, y_0, t_lista)

